# frozen_string_literal: true

# RSpec.describe Order do
  # context 'pruebas de las lineas' do
  #   it 'line added' do
  #     order = Order.new
  #     line_item = LineItem.new(:item => Item.new(:price => Money.new(1.11, :USD)))
  #     order.add_entry(line_item)
  #     expect(order.lines[0]).to eq(line_item)
  #   end

  #   it 'al iniciar una orden no tiene lines' do
  #     order = Order.new
  #     expect(order.lines.size).to eq(0)
  #   end
  # end

  # it "sums the prices of its line items" do
  #   order = Order.new
  #   order.add_entry(LineItem.new(:item => Item.new(
  #     :price => Money.new(1.11, :USD)
  #   )))
  #   order.add_entry(LineItem.new(:item => Item.new(
  #     :price => Money.new(2.22, :USD),
  #     :quantity => 2
  #   )))
  #   expect(order.total).to eq(Money.new(5.55, :USD))
  # end
# end
